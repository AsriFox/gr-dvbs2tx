id: dvbs2tx_ldpc_bb
label: LDPC Encoder
category: '[DVB]/DVB-S2 Transmitter'

inputs:
- domain: stream
  dtype: byte

outputs:
- domain: stream
  dtype: byte

templates:
  imports: from gnuradio import dvbs2tx
  make: dvbs2tx.ldpc_bb()

file_format: 1
